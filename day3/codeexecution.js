
function square (num){
    var ans = num * num
    console.log(ans);
    return ans
}
var square1 = square(2)
var square = square(4)

/* 
from this example we can see how javaScript code is executed .
Java execution contex is created first (memory allocation stage)
execution context is created in 2 phase
*/
